{
  "name": "baxterkemp.com",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "node scripts/setup-config.mjs",
    "install-hook": "node scripts/install-global-hook.mjs",
    "sync": "node scripts/sync-contributions.mjs",
    "postinstall": "npx playwright install chromium || echo 'Playwright Chromium install skipped — run: npx playwright install chromium'"
  },
  "dependencies": {
    "playwright": "^1.54.2"
  }
}
